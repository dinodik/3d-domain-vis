{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/main.ts --bundle --outfile=dist/bundle.js && cp src/index.html dist/index.html && cp src/styles.css dist/styles.css",
    "watch": "npx esbuild src/main.ts --bundle --outfile=dist/bundle.js --watch",
    "clean": "rm -r dist/ node_modules/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/three": "^0.166.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "three": "^0.166.1"
  }
}
